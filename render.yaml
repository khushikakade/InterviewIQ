services:
  - type: web
    name: interviewiq
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT --timeout 300 --workers 1 main:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
